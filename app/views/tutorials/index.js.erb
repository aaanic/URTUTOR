// const select = document.querySelector('select')
// const cards = [];

// const generateCards = () => {
//   const rawCards = document.querySelectorAll('.card');
//   cards.forEach((card) => {
//     const some = new Obj;
//     some.html = card;
//     some.category = card.querySelector('.category').innerText;
//     cards.push(some);
//   })
// }

// const filterCards = (filter) => {
//   cards.forEach((card) => {
//     if (card.category == filter) {
//       card.html.style.display = 'block';
//     } else {
//       card.html.style.display = 'none';
//     }
//   })
// };

// const getBackAllCards = () => {
//   cards.forEach((card) => {
//     card.html.style.display = 'block';
//   })
// };

// generateCards();
// select.onchange = function(){
//   getBackAllCards();
//   const fuckYes = select.selectedOptions[0].innerText;
//   filterCards(fuckYes);
// };

